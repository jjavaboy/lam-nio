<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans        http://www.springframework.org/schema/beans/spring-beans.xsd        http://code.alibabatech.com/schema/dubbo        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
 
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="lam-dubbo-consumer" owner="skylin" />
 
    <!-- 使用multicast广播注册中心暴露发现服务地址 -->
    <!-- <dubbo:registry address="multicast://224.5.6.7:1234" /> -->
    
    <!-- 使用zookeeper注册服务 -->
    <dubbo:registry protocol="zookeeper" address="192.168.204.127:2181" /><!-- 192.168.20.110:2181,192.168.20.111:2181 -->
    
     <!-- 监控中心 dubbo的monitor-simple模块 -->
    <dubbo:monitor protocol="dubbo" address="127.0.0.1:7070" />
 
    <!-- 引用的服务 -->
    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
    <dubbo:consumer timeout="10000" check="false" />
    <dubbo:reference id="loginService" interface="lam.dubbo.api.LoginService" />
    <dubbo:reference id="userService" interface="lam.dubbo.api.UserService" />
 
 	<bean id="lamController" class="lam.dubbo.consumer.controller.LamController" init-method="start">
 		<property name="loginService" ref="loginService"/>
 		<property name="userService" ref="userService"/>
 	</bean>
</beans>